# Working on Space Opera RPG

Read README.md, docs/current-design.md, docs/open-decisions.md, and the active slice before work. Update the Desktop tracker and local slice state before beginning a new engineering continuation. Preserve owner corrections as the active interpretation; do not promote suggestions into canon.

B1 chapter planning is complete in `docs/chapter-implementation-packet.md`; B2 is technically complete in `docs/slices/B2-playable-foundation.md` with evidence in `evidence/B2/README.md`. B2.5 command-adventure play is technically complete and explicitly owner-accepted in `docs/playtests/B2.5-owner-acceptance.md`; B3 connected world is technically complete in `docs/slices/B3-world-and-tasks.md`, with evidence in `evidence/B3/README.md` and owner review pending; B4 equipment, power progression and preparation is technically complete in `docs/slices/B4-progression-and-preparation.md`, with evidence in `evidence/B4/README.md` and owner acceptance pending; B5 is technically complete in `docs/slices/B5-owned-home.md`, with exact evidence in `evidence/B5/README.md` and owner acceptance pending; B6 is technically complete in `docs/slices/B6-companions-and-care.md`, with exact evidence in `evidence/B6/README.md` and owner acceptance pending; B7 is technically complete in `docs/slices/B7-expedition-and-resolution.md`, with exact evidence in `evidence/B7/README.md` and owner acceptance pending; B8 is technically complete in `docs/slices/B8-player-experience.md`, with exact evidence in `evidence/B8/README.md` and owner acceptance pending; B9 is technically complete in `docs/slices/B9-standalone-mac.md`, with exact standalone package evidence in `evidence/B9/README.md`; B10 owner review is prepared but unstarted; see `docs/next-task.md`. S05 remains a separate unrun prototype review and does not block this handoff.

Product priorities: **A1 is modern text-adventure command play; A1a is story and gameplay.** Typed natural language, precise movement, prompts/choices and grounded narration form the main player experience. B2 is technically complete; `docs/slices/B2.5-command-adventure.md` records the command/story implementation; B3 extends it; B4 delivers progression and preparation; B5 delivers the owned private home, furnishings and storage; B6 delivers companion support, training and recoverable care; B7 delivers the full expedition and persistent chapter resolution; B8 delivers character setup and full-chapter player experience; B9 delivers the standalone personal Mac app; no engineering slice is active and B10 owner review is unstarted. AI/local-model selection remains an assessed implementation option. Preserve these priorities throughout later chapter work.

Keep one slice active. Complete its bounded deliverables and relevant checks; record exact artifact identity and owner feedback separately. Do not equate a concept image, a passing test, or documentation with accepted playable quality. S01 selected Godot 4.6.2 and an illustrated cutout workflow for S02: read docs/technical-approach.md before implementation. S02 has a runnable visual sample and recorded owner feedback with limitations; no polished full game has been accepted.

Ask only relevant unresolved questions. Use short batches with four suggestions plus add-your-own for most open-ended questions; yes/no questions need no artificial options. Do not restart discovery or ask all reference-library questions.

This is a personal Mac single-player first version. Preserve spatial interaction and the selected illustrated direction. Feed access is outside-audience only. Temporary shelter does not imply ownership/privacy. The first experiment remains damage-free. September 21 beta scope permits recoverable pet injuries requiring care and excludes permanent pet or recruit death; see current-design.md. Do not re-ask the resolved injury question. Purchases/publication/external messages require the applicable authorization; routine reversible project work does not need repeated permission.

When implementing, choose a project-local validation entry point and document it. Avoid inventing test results, release status, dates, or numeric balance as confirmed design. No subagent delegation is requested by these instructions.

## Current maintenance delivery

UI-05 is technically complete in `docs/slices/UI-05-accessible-mac-update.md`, with exact updated package evidence in `evidence/UI-05/README.md`. The separately installed **Space Opera RPG Updated Beta.app** includes M1 error handling, M2 security hardening, UI-04 clarity and whole-interface 100%/125% text. It uses its own save namespace. Original B9 app/evidence and B10 handoff remain preserved; B10 is unstarted and updated-candidate owner acceptance is pending. No engineering slice is active. See `docs/next-task.md` for current delivery; do not repeat the completed UI-04 suggestions.

M3 SSOT enforcement is technically complete on source; see `docs/ssot.md`, `docs/slices/M3-ssot-enforcement.md` and `evidence/M3/README.md`. Source has advanced beyond frozen UI-05; installed packages remain unchanged. Current source Run starts player_experience; chapter startup uses one most-derived save factory. No engineering slice is active.

M4 repository cleanup is technically complete. Use `docs/development.md` and `docs/README.md`; required synthetic inputs live in tracked `tests/fixtures`, not ignored evidence. Interface capture orchestration is shared in `scripts/interface_capture.py`. `bash scripts/validate.sh M4` verifies an evidence-free export of the current working source. No engineering slice is active; packages remain frozen.

M5 CI readiness is complete; hosted CI passed for `3bd7d5ac499c1e3e2922f4a8bad6602930749b59` (run `36079157238`). See `docs/ci.md` and `docs/next-task.md`; that source result is not package qualification. Preserve managed CodeQL and stable `Source checks (macOS)` job name. Ordinary PR CI runs focused M4 source checks, never packaging/signing. No engineering slice is active.

M6 documentation accuracy is complete. Public setup and current behavior live in README and the guides linked by `docs/README.md`; `docs/history/README.md` indexes planning/candidate records. Keep chronology and private workspace routines out of public-facing guidance while preserving existing records and actual API/config identifiers. No engineering slice is active.
