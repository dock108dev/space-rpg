# Native Mac review — engineer-operated synthetic state

Reviewed in the installed Godot 4.6.2 on the owner's Mac using ordinary CUA mouse and keyboard input, the root `Launch B2.command`, and a separately created `/tmp/space-opera-b2-native-final-*` save directory. No owner/practice save was read or changed. This is engineering review, not an owner session.

The final native process was launched from `/tmp`. Its B2 title and 1280×720 game canvas were visible in the Retina window. The UI exposed WASD/arrows, E, Enter, right-click, assigned walking, Continue and pause/save/quit. Both the cool glass controls and world labels were legible; the repaired contextual reward/recruit row stayed clear of Pause.

The native sequence began fresh on the exploratory candidate, inspected all three demonstrations, chose blast, reclaimed assigned walking with D, used W/E to enter the assessment, and advanced the creature with Enter. Targeting then exposed an actual input defect: the delivered native event correctly pointed at the creature but polling the system cursor returned unrelated coordinates. The retained diagnostic is in `../native-targeting/launch.log`. B2 now derives targets from the event position, transformed into scene coordinates. Historical S04 input code is unchanged.

On the repaired final runtime, Continue resumed that ordinary, unmodified synthetic encounter. Native mouse blasts reduced creature health from 6 to 3 to 0; E collected the package and taught fetch. Assigned walking reached the actual doorway, E entered the distinct shelter, the field-lamp reward was selected, D approached the traveler and Join me recruited it. Assigned walking and E returned all three actors to the cleared concourse. Pet: fetch showed physical outbound and return travel; the protagonist stayed in place and exactly one kit appeared. E returned to shelter. Escape opened the stable pause menu; Save and quit ended the process normally.

A new launcher process started at the opening screen. Continue latest visibly restored shelter, health 6, one kit, field lamp and joined traveler. An ordinary Save then produced snapshot15. `restart-comparison.json` compares it to the pre-exit snapshot14: every gameplay field, including power, events, location and both actor positions, is exactly equal. Only snapshot sequence changed. A final normal Save and quit closed the verification session. Native launch/restart logs contain no ERROR or leak output.

Focus changes consistently froze the game until explicit Escape or Continue. Window close at a selected/stable state opened the save-and-quit pause menu. The first native launch retained an unrelated CoreAudio startup error and inherited VIS-001 title; both were repaired. B2 has no authored sound and its launcher explicitly uses the Dummy audio driver. An attempted detached launch exited with the tool's parent process and supplied no gameplay evidence; its empty log/manifest remains in native-attempt-02.

The native runtime/art/launcher bytes match final automated evidence; only the test script and runner changed afterward. `source-reconciliation.json` lists those exact differences. The fresh final automated power journeys and moving tour bind the final full manifest. This native review supplies real input/Retina observations; it is not mislabeled as a fresh complete playthrough of every branch on the final revision.

Observed limits: human cutout gait remains stiff; the pet uses an unarticulated bob/flip body; the recruit and shelter vectors are more graphic than the inherited raster illustrations. The recruit remains readily distinct and no duplicate limbs or missing assets were seen. The safe party has no companion combat, injuries or care. No owner aesthetic or enjoyable-play verdict is inferred.
