# M6 — Documentation accuracy

Status: TECHNICALLY COMPLETE.

Rewrite active references for repository users without internal planning context. Verify current commands, values and links; retain precise identifiers where they are real interfaces or provenance. Preserve planning/history and immutable evidence without making them setup prerequisites.

Rewrote README and active development, architecture, validation, CI, error handling, security and UI references. Added current player guide and a history index pointing to existing records without moving or rewriting immutable evidence. Previous player guide is explicitly historical. Removed external template/private-workspace assumptions and planning progress from active docs and contextual comments. Preserved real filenames, selectors, schema/environment names and diagnostic identifiers.

Small executable change: source window title no longer displays an internal milestone. Capture CLI descriptions were also corrected. Source validation (`bash scripts/validate.sh M3`) passed 33 assertions, three launcher cases, import/startup and isolation in `evidence/M3/run-20260925T004345801002Z`. Python compilation, three safe CLI-help calls, 37 local Markdown links, command/config-path checks and whitespace checks passed. No full matrix, native walkthrough, hosted run or packaging/signing.

Explicit limits: command grammar is bounded; no server/model/cloud saves or automatic migration; reduced-motion/transparency settings are unimplemented design targets; general accessibility compliance and packaged/hosted release readiness are not established by this pass. Both frozen installed bundles and archives remain byte-identical. No engineering slice is active; owner acceptance remains pending.
